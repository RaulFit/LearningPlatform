<% if params[:available] %>
  <%= form_with(url: courses_path(available: true), method: 'get') do %>
    <div class="relative flex w-1/3 border border-gray-300 rounded-lg bg-gray-50 ">
      <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
        <svg class="w-4 h-4 text-gray-500 dark:text-gray-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
          <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m19 19-4-4m0-7A7 7 0 1 1 1 8a7 7 0 0 1 14 0Z"/>
        </svg>
      </div>
      <%= text_field_tag :search, params[:search], class: "flex-grow p-4 pl-10 text-sm text-gray-900 bg-gray-50 rounded-l-lg focus:outline-none", placeholder: "#{t :search_courses}" %>
      <%= hidden_field_tag :available, true %>
      <%= submit_tag "#{t :search}", class:"flex-none px-4 py-2 text-white bg-indigo-700 hover:bg-indigo-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-r-lg text-sm" %>
    </div>
  <% end %>
<% elsif params[:authored] %>
  <%= form_with(url: courses_path(authored: true), method: 'get') do %>
    <div class="relative flex w-1/3 border border-gray-300 rounded-lg bg-gray-50">
      <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
        <svg class="w-4 h-4 text-gray-500 dark:text-gray-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
          <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m19 19-4-4m0-7A7 7 0 1 1 1 8a7 7 0 0 1 14 0Z"/>
        </svg>
      </div>
      <%= text_field_tag :search, params[:search], class: "flex-grow p-4 pl-10 text-sm text-gray-900 bg-gray-50 rounded-l-lg focus:outline-none", placeholder: "#{t :search_courses}" %>
      <%= hidden_field_tag :authored, true %>
      <%= submit_tag "#{t :search}", class:"flex-none px-4 py-2 text-white bg-indigo-700 hover:bg-indigo-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-r-lg text-sm" %>
    </div>
  <% end %>
<% elsif params[:enroled] %>
  <%= form_with(url: courses_path(enroled: true), method: 'get') do %>
    <div class="relative flex w-1/3 border border-gray-300 rounded-lg bg-gray-50 ">
      <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
        <svg class="w-4 h-4 text-gray-500 dark:text-gray-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
          <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m19 19-4-4m0-7A7 7 0 1 1 1 8a7 7 0 0 1 14 0Z"/>
        </svg>
      </div>
      <%= text_field_tag :search, params[:search], class: "flex-grow p-4 pl-10 text-sm text-gray-900 bg-gray-50 rounded-l-lg focus:outline-none", placeholder: "#{t :search_courses}" %>
      <%= hidden_field_tag :enroled, true %>
      <%= submit_tag "#{t :search}", class:"flex-none px-4 py-2 text-white bg-indigo-700 hover:bg-indigo-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-r-lg text-sm" %>
    </div>
  <% end %>
<% end %>
