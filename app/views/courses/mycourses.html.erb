<nav class="bg-indigo-400 inline-flex items-center fixed w-full z-20 top-0 start-0 border-b border-gray-200">
  <div class="max-w-screen-xl inline-flex items-center flex-wrap justify-between mx-auto p-4">
  <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-sticky">
    <ul class="flex flex-col p-4 md:p-0 mt-4 font-medium border border-gray-100 rounded-lg  md:space-x-8 rtl:space-x-reverse md:flex-row md:mt-0 md:border-0 dark:bg-gray-800 md:dark:bg-gray-900 dark:border-gray-700">
      <li>
        <a href="#" class="rounded-md px-3 py-2 text-black hover:bg-indigo-700 hover:text-white">Owned courses</a>
      </li>
      <li>
        <a href="#" class="rounded-md px-3 py-2 text-black hover:bg-indigo-700 hover:text-white">Registrations</a>
      </li>
    </ul>
  </div>
  </div>
</nav>

<div class="flex flex-col p-5 lg:px-48 lg:py-11">
    <% @courses.each do |course| %>
      <% if course.author_id == current_user.id %>
        <div class="bg-gray-100 p-5 mt-10 rounded-lg">
            <h1 class="font-bold text-2xl mb-2"><%= course.title %></h1>
            <p class="my-3"><%= course.description %></p>
            <%= link_to "Read more...", course_path(course), class:"text-white font-semibold bg-gray-600 hover:bg-gray-700 p-2 my-1 rounded" %>
            <%= link_to "Add lesson",  new_course_lesson_path(course), class:"text-white font-semibold bg-gray-600 hover:bg-gray-700 p-2 my-1 rounded" %>
            <%= link_to "Edit", edit_course_path(course), class:"text-white font-semibold bg-indigo-600 hover:bg-indigo-800 p-2 my-1 rounded" %>
        </div>
      <% end %>
    <% end %>
</div>
